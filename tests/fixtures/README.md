# Protocol fixtures

Small protocol samples for adapter tests. Identifiers are synthetic, and the
recall result is a fabricated project fact. These files do not supply runtime
memory or credentials. Usage values exercise parser behavior, not model benchmarks.
