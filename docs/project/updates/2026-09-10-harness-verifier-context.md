# DREAM-035 — Verifier context and current-request priority

Recorded: `2026-09-10T19:37:39-05:00`
Work items: `DREAM-035`
Outcome: `verified`
Actor: Codex lead; read-only intent_adaptation_audit; fresh gate planned.

## Request

Continue the authorized iteration after [numeric validation](2026-09-10-harness-numeric-configuration.md).
Phase6 acceptance in the plan
protects current user intent while retaining attributed, optional verifier context.

## Changes

Scope and544-hash baseline /tmp/dream035-verifier-baseline.json recorded before
editing. Preserve earlier accepted phases and the pre-existing dirty tree.
Candidate1 implemented; fresh review pending, source frozen. Separate named assistant context plus complete optional
message removal under compact policy is the chosen bounded correction.

## Validation

Root graph discovery again returned Transport closed; focused reads followed.
Read-only CPU audits reproduced lost user attribution, large-report refusal and
residual-stub refusal near a context boundary. Error policy's refusal is preserved.
No model, GPU, provider, benchmark, build, install or external transmission.

## Unfinished work

Phase6 passed its fresh gate; fixture compatibility passed its cleanup judge. No owned fixture process/lock active.
Native/live/clean-install qualification and production acceptance remain unperformed.

## Next steps

Start phase7 explicit delegate completion/primary-choice validation with a new
contract and baseline, then rerun broader model-free compatibility.

## Files changed

- `dream/core/backends/openai_compat.py`
- `tests/test_verifier_fork.py`
- `tests/test_durable_autonomy.py`
- `tests/test_verifier_context.py`
- `docs/runtime-controls.md`
- `docs/harness-review-packet.md`
- `docs/superpowers/plans/2026-09-10-harness-adaptation-loop.md`
- `docs/project/CURRENT.md`
- `docs/project/MASTER_PLAN.md`
- `docs/project/DECISIONS.md`
- `docs/project/updates/2026-09-10-harness-verifier-context.md`

Phase6 candidate1:8 regression cases failed before implementation. Separate
named assistant context now precedes the untouched user. Compaction removes
optional reports completely and calls existing labeled working-note capture.
Explicit error policy preserves context/refusal. A proactive tool-pair fixture
then failed: name alone could remove an assistant tool call. Requiring no tool
calls corrected it, preserving pairing. The initial sandbox run stalled after28
cases and timed out; approved CPU-fixture retry passed68 tests2.19s. Expanded
verifier/compaction/model/salvage/filing/stream/effort/tuning suite passed167 in3.17s.
No live provider/model/GPU/benchmark operation. Fresh intent/adaptation review
pending; production source frozen. External review packet refreshed with accepted
phases and this candidate's unaccepted status; it was not transmitted.

Broader compatibility check: the full model-free pytest suite used a disposable
DREAM_ROOT and no --with-models, but exited124 at its240-second bound after about
634 passing dots; it is not a full-suite pass. Collection separately found2599
cases. The next collected case was durable interruption/reconciliation. Isolated
10-second retry also stalled: its fake Worker.interrupt only sets a boolean while
its act coroutine waits forever on an unreferenced Event. It depended on raw task
cancellation removed by phase3. Same cleanup judge is reviewing a test-only
cooperative-stop correction; all original ledger/no-replay assertions will remain.
Root graph query stalled during this diagnosis and was terminated before focused
reads; no mutation occurred in that graph call. Seven changed Python modules
parsed successfully. Current phase6 tracking passed27 records/9changed paths.

Phase6 compatibility scope adds tests/test_durable_autonomy.py before its edit.
The same cleanup judge confirms the proposed test-only stop Event models the
accepted cooperative-interruption contract. Retain all existing uncertainty,
reconciliation and no-replay assertions, add cleanup-settled-before-return, and
leave ineffective-interruption ownership tests unchanged. No production code is
changed for this fixture correction. The implementation still needs its gate.

Final phase6 verdict: PASS. Fresh judge independently passed166 focused tests in
1.67s (one browser case deliberately deselected),45 payload/adaptation probes and
16 exact-boundary probes across2048–8192-token windows. Actual bounded notes retained
originating turn/path/error labels; effort, request counts and tool pairs remained
intact. Its sandbox run timed out after37 cases; one approval-review timeout
occurred before execution, then the permitted retry passed. Lead had separately
passed all167 including the browser case. Backend reviewed hash:
10b2d97df79c28d90aee7b355f6df505312e47c048db7aa7eb3a9b4a8cca71c0.

The durable fixture correction passed51 lead tests5.08s and the same cleanup
judge independently passed51 in5.47s, including ineffective-interruption controls.
The earlier full-suite timeout remains incomplete evidence; rerun is queued after
the next production correction. No test assertion was removed. No fixture process
or lock remains. Final tracking passed27 records/11changed paths.

Read-only delegate protocol audit observed56 synthetic cases through actual
_run_subagent/_subagent_loop and fake HTTP transport. Explicit length/filter/unknown
finishes could dispatch structured/XML calls and report success; explicit secondary
choices could replace choice0. Both reproduced remotely and with temporary real
local coordination. Complete JSON bodies released their leases normally. Missing/
null finishes are existing compatibility behavior, distinct from these explicit
failures. Phase7 needs a new contract, baseline and fresh protocol gate.
