# DREAM-035 — Audit adaptation and verification contracts

Recorded: `2026-09-10T23:06:08-05:00`
Work items: `DREAM-035`
Outcome: `implemented`
Actor: Codex lead verifier audit and docs; fresh Astra model-adaptation and execution-prerequisite auditors.

## Request

Continue the active iterative hardening goal after phase14's verified checkpoint.
Previous goal turn is progress: accepted source correction and full2858-test pass.
This pass examines model adaptation, execution-prerequisite truthfulness and
verifier behavior using source and bounded synthetic probes. No live qualification.

## Changes

Phase15 accepts only a complete standalone PASS marker from a full verifier sweep.
Additional or contradictory text remains visible; failure flags and next-turn user
priority remain intact. Exact two-file correction passed a fresh independent Astra
gate and is integrated. Runtime controls and the external feedback packet match.

Baseline /tmp/dream035-adaptation-audit-baseline.json has563 hashes. Existing dirty
work was preserved. Read-only Astra audits also established two Active settings
attribution defects and saved execution-prerequisite reporting gaps, with actual
request correctness and safe execution refusal retained in their bounded probes.

## Validation

Current entry docs and register read. Graph-first lead search failed Transport
closed, then focused source reads located verifier handling. A guessed runtime_profiles.py
path was absent; no result relies on that path. Fresh audits use graph-first tools.

Lead20s-bounded pureCPU probe invoked actual OpenAICompatBackend._verifier_sweep
on a synthetic host whose subagent callback returns fixed text, without constructing
an adapter or invoking a provider/tool. Observed exit0: PASS, PASS followed by a
navigation defect on a second line, PASSENGER text is clipped., and PASS? I could
not inspect the page. all emit verifier: PASS (fixture.html) with no queued findings.
The parser uses startswith("PASS"); contradictory and unavailable results can thus
be silently discarded. This is contract evidence, not model-quality measurement.

## Unfinished work

Phase15 source correction and independent gate are complete. The unchanged browser
case and full suite await consolidated integration after the next gated corrections.
Phase16 has pre-edit display-attribution acceptance; the readiness proposal remains
separate. No live model/provider, hardware, native, build/install or owner acceptance.

## Next steps

Take a new phase16 baseline and current-source isolation from accepted phase15.
Correct only Active settings reporting, then fresh Astra gate. Implement prerequisite
reporting under its separately declared finite contract. Root reconciles shared
tracking and consolidated verification. No phase15-owned process remains.

## Files changed

- `dream/core/backends/openai_compat.py`
- `tests/test_verifier_fork.py`
- `docs/runtime-controls.md`
- `docs/harness-review-packet.md`
- `docs/superpowers/plans/2026-09-10-harness-adaptation-loop.md`
- `docs/project/CURRENT.md`
- `docs/project/MASTER_PLAN.md`
- `docs/project/updates/2026-09-10-harness-adaptation-audit.md`

Phase15 acceptance recorded before source edits in the shared plan: only complete
trimmed PASS is clean; preserve contradictory/extra/ambiguous report text and user
priority; clarify sweep prompt, retain scheduling/directed-check behavior. Existing
PASS-with-commentary fixture requires canonical-marker correction with explicit
negative regression. Root will give an isolated Astra author the declared paths,
then a NEW fresh read-only judge. Other two audits remain read-only.

Phase15 isolation prepared from514 current-source files with dependencies linked;
manifest /tmp/dream035-verifier-marker-isolation.json. New implementer spawn was
rejected by the runtime thread limit before starting. Both fresh read-only audits
remain active; no source edit, workaround or unperformed contributor claim. Lead
will dispatch after an audit completes or reuse a completed non-gate contributor.

Execution-prerequisite audit completed and freed a contributor slot. Subsequent
new astra_verifier_marker_impl dispatch succeeded; it owns only isolated phase15
paths. No source change in main. Fresh execution report:
/tmp/dream035-execution-prereq-audit.md, nine synthetic cases passed with zero
workers launched. Containment refusal remains enforced. Two P2 readiness gaps:
model catalog omits known prerequisite evidence; Controls presents cached shell
availability without a separate script-runtime distinction. A P3 offline diagnostic
not_run coverage gap is also recorded. No bypass or live model retry loop proven.
These become follow-up qualification/correction scope after the verifier marker;
no probes, host fallback, broad tool removal or permission changes are authorized.

Model adaptation audit complete: /tmp/dream035-model-adaptation-audit.md, with
actual backend MockTransport proof. Provider label extended reaches the request
and Performance but Active settings marks it unknown. Explicit4096 reported
context bounds admission but Active settings labels it an assumption. Four other
request-adaptation cases passed, including conflict and model-change behavior;
telemetry attempts0. No outgoing request defect or live provider support claimed.
The terminal profile-retention concern matches documented behavior and is not
a finding. Next bounded display-only correction should retain all request/admission
logic and verify current/prepared effort plus measured/reported/configured sources.

Phase15 candidate frozen after red7 expected failures/6 controls passed, then33
focused tests passed/1 explicit existing-browser deselection in0.67s, no warnings.
Author /tmp/dream035-verifier-marker-impl.md. Root checked514 baseline entries,
exactly2 candidate changes and unchanged main originals; exact -lead.diff and
-lead-hashes.json. NEW astra_verifier_marker_gate now independently verifies the
marker contract and next-turn preservation. Main remains unchanged. No browser
case was silently counted as pass; full integration remains a later lead check.

Fresh phase15 judge progress:33 tests passed/1 explicit browser deselection in0.38s;
exact514-file candidate integrity and main originals verified. Independent probes
completed; final count/report and real delegate-loop fake-response checks remain.
No gate verdict or integration claimed yet. Root recorded separate phase16
display-only acceptance from the completed adaptation audit. Execution auditor is
refining a bounded future readiness proposal in/tmp only; no source edit or host
probe. Root graph attempts still return Transport closed, with focused known-file
reads after each attempt.

Final phase15 fresh gate PASS:33 focused tests/1 explicit browser deselection in
0.38s plus113 independent probes in0.51s. Report /tmp/dream035-verifier-marker-gate.md.
Original prefix/Unicode counterexamples fail old source and retain reports on
candidate. Real delegate-loop MockTransport, scheduling, cancellation, failedflag,
current-request priority and phase6 compact/error contracts passed. Judge's first
expanded probe had10 fixture errors (missing fake base URL/profile); corrected
fixture passed without source edits, both logs retained. No candidate finding.

Lead checked old/new hashes then integrated exactly two accepted files. Runtime
controls docs and manual feedback packet describe the precise marker contract.
Audits and phase15 scope implemented; phase16 display-only acceptance is next,
followed by the finite prerequisite-readiness proposal. Source-only prototype
reports do not establish live model quality. Full verification is consolidated
after the following gated corrections rather than repeating the same broad suite
between disjoint bounded phases. Latest full2858 pass remains prior-source evidence.

Final phase15 tracking passed36 dated records and all8 changed source paths.
