# DREAM-035 — Baseline evidence and live effort controls

Recorded: `2026-09-11T10:22:47-05:00`
Work items: `DREAM-035`
Outcome: `implemented`
Actor: Codex lead; read-only Astra session and UI auditors.

## Request

Owner authorized reviewing yesterday's sessions where available and moving forward
with establishing a baseline, and requested confirmation of a live UI reasoning/
effort control. September10 means local America/Chicago, UTC05:00 September10 to
05:00 September11. Relevant historical reads are authorized; no replay or mutation.

## Changes

Lead owns baseline documentation, synthetic result captures and shared tracking.
No production change is currently justified. Acceptance before work: distinguish
retrospective uncontrolled observations from deterministic framework baseline and
unperformed model-quality comparisons; record exact case/grader/source identity,
actual outputs and missing metadata; reuse existing telemetry/evaluation paths;
verify real UI control location, capability limits and effective timing without
changing the running owner application. No invented task success or speed claims.

Preserved dirty tree and589-hash baseline
/tmp/dream-baseline-live-effort-20260911-baseline.json. Session auditor owns only
/tmp/dream-yesterday-baseline-audit*; UI auditor only /tmp/dream-effort-live-ui-audit*.
Fresh UI audit is source/test-only. A new session-audit thread hit the thread cap,
so the existing read-only efficiency reviewer was reassigned; no new-thread claim.

## Validation

Root graph query timed out in automatic permission review and its cell was
terminated without subsequent shell reads launching. Focused known-source reads
followed; this was an approval timeout, not an unsafe-action rejection. Both
agents use graph discovery first. Existing venv and model/network denials apply
to finite synthetic baseline capture. No live model/provider/GPU/owner UI operation.

## Unfinished work

None within historical/deterministic baseline and existing effort verification scope.
Model task-success calibration remains unmeasured until actual model choices can
be independently graded. Per-turn effort is missing historically; live thinking
on/off would require a separate supported runtime contract. No benchmark sweep.

## Next steps

Investigate repeated/inapplicable verifier calls against actual task acceptance.
Before effort comparison, capture effective per-turn model/effort and graded output
for a frozen targeted case. Preserve original private stores and all failed attempts.

## Files changed

- `docs/project/CURRENT.md`
- `docs/project/MASTER_PLAN.md`
- `docs/project/updates/2026-09-11-baseline-and-live-effort.md`
- `docs/harness-baseline-2026-09-11.md`
- `docs/harness-evaluation.md`
- `docs/runtime-controls.md`
- `docs/superpowers/plans/2026-09-10-harness-adaptation-loop.md`
- `artifacts/harness-baseline/2026-09-11/native-records.json`
- `artifacts/harness-baseline/2026-09-11/native-grade.json`
- `artifacts/harness-baseline/2026-09-11/manifest.json`
- `artifacts/harness-baseline/2026-09-11/retrospective-summary.json`

Existing UI audit completed:95 focused passes,91 runtime in0.78s and4 software
Chromium in3.44s. Root read the full report/result log and viewed the screenshot.
Main effort and advisor effort are visible selectors; idle-only Council Apply
changes next-turn effort. Same-main MachX/HTTP preserves backend/loaded weights;
Anthropic reconnects its SDK. Thinking on/off remains a separate loading control.
No duplicate control or source edit is required for existing effort adjustability.
Runtime docs now describe this path and correct the older loading-only low guidance.
Agent's initial sandbox pytest stalled after23 dots and was interrupted exit130;
normal-threading rerun passed unchanged. All UI agent sessions ended. Fixed old
screenshot path was preserved through an output-path-only diagnostic plugin.

Root's first guarded native capture timed out after30s in the sandbox, exit124,
with empty stdout and no exported record. Exact empty log/result preserved under
/tmp/dream-baseline-capture-sandbox*. Same capture with normal threading exited0
in0.25s and graded5/5 existing native cases. This elapsed time is not a model-speed
measurement. Guarded import/network/process denials remained active. No inferred
root cause beyond the observed different execution environments. Existing grader
and actual Engine/profile selected tests then passed20 in2.30s,zero skips/warnings;
session83961 terminal. Log /tmp/dream-baseline-regression.log. No full suite needed
for these unchanged-source baseline and UI checks.

Historical review completed under read-only SQLite/query_only and bounded matching
runtime logs:3 sessions started,2 with activity,109 stored records,7 runtime timings.
MachX2 completed/2 error; Codex3 completed. No independent task grades; per-turn
effort absent. Existing sanitized launch review provides low effort at launch only.
No private transcript content, session IDs or absolute model paths copied into the
repo. Root read the sanitized JSON, checked role counts, timing totals and exact
verification fractions, then copied it unchanged. Manifest hashes the aggregate.

The two completed MachX turns have verification663.6689/elapsed970.6144 and
414.3349/540.1782 seconds,68.4% and76.7%. This supports first investigating scoped
verifier calls, not claiming an equivalent speedup or suppressing required review.
Cache usage is already reported. Tasks/providers/instrumentation differ; no provider
comparison, crash cause, RAM finding, quality score or best-effort inference.

Baseline manifest freezes current case/grader/source identity and results, explicitly
null model-quality metrics and prospective per-task recording fields. Current source
is newer than historical sessions; fixes cannot be credited retroactively. Both
original failed sandbox attempts remain preserved with their successful unchanged
normal-threading follow-ups. No full regression repeated; no production source,
test or config change was made. Final independent baseline/prose review follows.

Independent UI auditor also reviewed the baseline without authoring it.31 read-only
stdlib coherence/hash checks passed: all source/artifact/evidence hashes,12 tools,
5 case grades,109 role totals, timing summaries/ratios and cache/request counts.
No provenance/quality overclaim found. Root applied the one wording correction:
manifest now names the interrupted effort-runtime CPU run, not the successful UI
browser run. Rechecked source/fixture/aggregate hashes after that metadata edit.

Final tracking passed44 dated records and7 changed source/doc paths. Four new
baseline JSON artifacts are explicitly listed above but excluded by the source
tracking filter. All589 existing baseline files were checked; only the five declared
existing docs differ, with the baseline report and dated update new. Source/test/
config files and earlier handoffs remain unchanged. All audit/test sessions ended.
