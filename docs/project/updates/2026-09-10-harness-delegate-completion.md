# DREAM-035 — Delegate completion and primary-choice safety

Recorded: `2026-09-10T19:42:19-05:00`
Work items: `DREAM-035`
Outcome: `implemented`
Actor: Codex lead; delegate_protocol_audit read-only; delegate_gate and fixture_compat_gate independent reviewers.

## Request

Continue after [verifier context](2026-09-10-harness-verifier-context.md).
Phase7's contract
extends explicit completion and selected-choice protection to nonstream delegates.

## Changes

Scope and546-hash baseline /tmp/dream035-delegate-completion-baseline.json recorded
before editing. Preserve pre-existing dirty work and accepted phases. Candidate
implementation and fresh protocol review complete, source frozen; reuse primary_choice and reject explicit unsuccessful
finishes before any response tools or clean summary. Omitted/null compatibility
and normal complete-body local lease release remain intentional.

## Validation

Read-only audit observed56 synthetic cases, including reproduced explicit-finish
and choice-order defects with remote and temporary local coordination. These are
observations, not56 passing safety assertions. Root graph returned Transport closed;
focused source reads followed. No live provider/model/GPU/benchmark/build/install.

## Unfinished work

Phase7 protocol and fixture gates passed; fifth full-suite run passed. No owned
fixture process or lock remains. Earlier timeout/failures remain evidence.
Native/live/clean-install and owner acceptance are unperformed.

## Next steps

Take a fresh baseline for the planned read-only vision/configuration status
contract, reproduce the status gap and obtain a new independent judgment.

## Files changed

- `dream/core/backends/openai_compat.py`
- `tests/test_delegate_completion.py`
- `tests/test_execution_foundation.py`
- `tests/test_monitor.py`
- `tests/test_cli_owned_lifecycle.py`
- `tests/test_contained_studio_script.py`
- `docs/runtime-controls.md`
- `docs/harness-review-packet.md`
- `docs/superpowers/plans/2026-09-10-harness-adaptation-loop.md`
- `docs/project/CURRENT.md`
- `docs/project/MASTER_PLAN.md`
- `docs/project/DECISIONS.md`
- `docs/project/updates/2026-09-10-harness-delegate-completion.md`

Phase7 candidate1: initial matrix reproduced62 failures with34 positive controls.
Reusing primary_choice and validating explicit finishes before recovery/dispatch
passed96 tests. Five additional false-valued/missing message-object cases then
failed; explicit object validation corrected them. A positive multi-round test
preserves earlier effects/usage while rejecting a later response. Lead combined
protocol/delegate/ownership/activity/coordination/model/verifier regression passed
227 tests in4.98s. No model/provider/GPU/benchmark operation. Fresh protocol review
pending; production source frozen. Full model-free rerun follows while the judge
checks the candidate. Prior full-suite timeout and durable fixture correction
remain documented in phase6; they are not relabeled as full-suite success.

Current offline diagnostic smoke: .venv/bin/python -m dream.diagnostics --json
exited0,28 central numeric settings valid and assets_missing=0. Wheel and records
explicitly not_run. No saved runtime state, provider or hardware inspected.
Full model-free rerun uses a new disposable DREAM_ROOT, verbose log
/tmp/dream035-full-after-delegate.log and360-second bound; it passed the corrected
durable fixture and is still running. Not yet a full-suite verdict. Read-only
vision adaptation audit starts from the previously recorded capability/tool-gating
mismatch; no vision implementation or live image call is authorized by that audit.

Full compatibility rerun exited3 after1753 passed,26 skipped and3 failed in105.78s,
then a pytest reporting INTERNALERROR. Three execution-foundation failures
reproduced in isolation: its synthetic tool context lacks the required session_id.
The monitor's passing width test globally replaces get_terminal_size with a
zero-argument lambda, crashing pytest when it supplies fallback. Expand lead scope
before editing to tests/test_execution_foundation.py and tests/test_monitor.py for
fixture-only interface compatibility. Add the matching synthetic session ID and
accept get_terminal_size's fallback argument; preserve every approval/single-use/
veto and layout assertion. No production fallback or safety check is weakened.
Fresh review of these corrections and the remaining full suite is still needed.

Phase7 protocol gate PASS: fresh judge independently passed227 tests5.06s and180
assertion-bearing probes, including isolated payload/effort/tool scopes, exact
primary selection, malformed state, legacy finishes, repaired tool IDs, prior
effects/usage, complete local idle release and uncertain-transport replay blocking.
Two restricted-sandbox runs stalled (exit130 manual interruption; exit137 bounded
verbose timeout); both remain incomplete evidence. Approved retry passed. Backend
hash f68119bd4fe7bd20007a0dfe386ce5893c180c74de734a0181139c4989fb0d9f.

Fixture corrections: lead55 execution/monitor tests passed4.59s with verbose
reporting. A separate fresh judge passed5 approval/layout cases0.48s and actual
lambda signature/dimension controls. Its initial sandbox2fail/3pass was trusted
interpreter refusal; approved retry passed. No assertions or production checks
were weakened. The full-suite third run remains active, not yet accepted.

Read-only vision audit reproduced supported metadata with disabled tool/images
and unsupported metadata with enabled tool/images. Engine does not supply that
metadata, and a synthetic MachX vision key is not a validated runtime schema.
This is a known reported/effective-behavior mismatch, not authorization to enable
image transport. A future inspection contract can expose effective behavior and
disagreement while preserving existing overrides. Actual MachX protocol/model
vision remains unqualified; no live image request was made.

Full third run completed:2665 passed,35 skipped,1 failed and7 warnings in237.64s.
The failure was CLI owned-tree idle fixture readiness (3-second path wait); isolated
unchanged module12 passed4.67s. A deterministic owned-process probe with .4-second
startup delay and .25-second idle limit reproduces premature expiry before the
fixture tree is ready; pausing only fixture setup allows the same idle expiry and
cleanup afterward. The original scheduling delay was not captured, so that exact
cause remains unverified. No production deadline change is proposed.

Expand compatibility scope before edit to tests/test_cli_owned_lifecycle.py. Its
idle-tree case will deliberately delay startup and pause its test setup until the
owned-tree ready signal, then restore the existing .25-second idle limit and all
cleanup/sibling-survival assertions. This tests idle cleanup independently of
startup scheduling. The existing production approval-pause behavior and dedicated
pause tests remain unchanged. A same-fixture-judge gate follows. Full-suite success
is still unclaimed; broader rerun follows accepted corrections.

CLI fixture deterministic probe passed2 cases1.67s. The modified12-case module
passed4.78s. Same fixture judge
reviews the delayed-startup/ready-signal correction. No production CLI timeout
or behavior was changed; no full-suite success is claimed yet.

Same fixture judge independently passed7 cases4.79s, including corrected idle
cleanup, both human-approval pause/resume forms, partial stdout/stderr activity,
and both deterministic delayed-startup controls. Verdict PASS for test robustness,
not proof of the original run's scheduling cause. Final model-free rerun active
with disposable root,360-second bound, skip reasons and log
/tmp/dream035-full-final-protocol.log. Source frozen; no inference benchmarks.

Five phase7 Python files parsed successfully without runtime imports. Final run
has progressed beyond87%; its20-second faulthandler diagnostic emitted during
the long script-fixture portion, and testing continued. Final exit/result remains
pending; do not treat the stack diagnostic itself as a failed or passed test.
Tracking passed28 records/11changed paths before this final reconciliation.

Fourth full run completed with2665 passed,35 skipped,1 failed and7 warnings in
238.69s. Verified skip breakdown:34 model-loading opt-in checks and1 migration-state
fixture (the live tree has already migrated). Failure moved to the Studio
busy-script recovery fixture: the second call logged alive but hit the inherited
1-second SCRIPT_TIMEOUT. Production default is30seconds. Preserve the first busy
loop's1-second limit, log assertion and6-second cleanup bound; scope its monkeypatch
only to that call, then verify actual alive output under the normal timeout with
a separate5-second caller bound. Add tests/test_contained_studio_script.py to lead
compatibility scope before editing. This separates deadline enforcement from
next-call recovery; no production timeout changes. Same fixture judge reviews.

Studio fixture follow-up: the unchanged isolated test also failed before emitting
its busy-loop log; the1-second end-to-end limit includes Python/Playwright/Chromium
startup. Recovery scoping alone then passed8 tests5.64s but did not eliminate that
first-call assumption. Same fixture judge reviewed an explicit acceptance adjustment:
use a3-second fixture deadline, retain timeout/log checks and the6-second cleanup
ceiling, add a3-second lower bound and reported-limit assertion. Normal recovery
retains its separate5-second caller bound. Production default remains30seconds.
This reduces startup sensitivity;3seconds still assumes browser startup completes
within that interval and is not a deterministic proof of the earlier scheduling
cause. A readiness-triggered runtime deadline would be a broader behavior change.
The proposal is accepted for implementation review; final gate remains pending.

Final Studio fixture correction passed8 lead tests7.19s. The same fixture judge
accepted implementation with2 independent timeout/recovery and cancellation/reaping
tests4.31s. Evidence: /tmp/dream035-studio-fixture-judge.log. Production timeout30
is unchanged; the3-second startup assumption remains explicit. Fifth full run:
disposable DREAM_ROOT, timeout360, pytest -q -ra --tb=short with20-second
faulthandler diagnostics, /tmp/dream035-full-reviewed-protocol.log. Result pending.

Final checkpoint `2026-09-10T20:16:30-05:00`: fifth full model-free run exited0,
2666 passed,35 skipped,7 warnings in240.60s. Skips are34 model-loading guards and
1 migration-state fixture. Warnings comprise Starlette TestClient/httpx and AnyIO
alias deprecations,2 synchronous tests marked asyncio, and3 SDK evaluator read-tool
shadow warnings. The20-second faulthandler dump did not prevent completion.
All earlier failed/incomplete runs above remain valid history. Phase7 protocol
and fixture gates are accepted engineering evidence, not owner acceptance or a
live compatibility/production claim. No model, GPU, benchmark, real build/install,
publication or external transmission. Tracking passed28 dated records/13changed
paths at final reconciliation. No owned fixture process or lock remains.
