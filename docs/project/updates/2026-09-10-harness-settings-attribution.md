# DREAM-035 — Settings attribution and execution readiness

Recorded: `2026-09-10T23:55:24-05:00`
Work items: `DREAM-035`
Outcome: `implemented`
Actor: Codex lead integration/docs and fixture correction; Astra isolated settings
and execution-readiness authors, with separate independent Astra judges and the
same phase15 reviewer for its fixture correction.

## Request

DREAM-035 phases16/17 correct the fresh audits' settings-attribution and saved
execution-prerequisite reporting defects, preserving request/admission and
execution authority. Phase15's accepted verifier-marker correction is retained.
The shared adaptation plan declared acceptance and disjoint scope before edits.

## Changes

Ten accepted source/test files are integrated from the two isolated candidates.
Current reported effort ladders are visible, determining context bounds are
attributed honestly, and unretained backend provenance is explicitly unattributed.
Saved shell checks are distinguished from current execution readiness; initial
model guidance is a bounded preparation snapshot. A compatible refusal subtype
identifies missing prerequisites without changing execution authority.

The consolidated suite exposed one stale background-verifier fixture. Its fake
response now uses exact PASS; every existing ordering/queue assertion is retained.
This additional one-literal scope was declared before editing. All eleven accepted
source/test hashes remain intact. Operator docs reflect the contracts; prior dirty
work, earlier handoffs and failed logs are preserved.

## Validation

- Phase16 initial independent gate FAILED on false measurement attribution. The
  SAME judge passed its correction:112 tests,4 browser deselections and477
  independent assertions. Report:/tmp/dream035-settings-attribution-correction-gate.md.
- Phase17 fresh gate PASS:66 tests,357 Python assertions and2191 Node cases.
  Report:/tmp/dream035-execution-readiness-gate.md. Exact eight-path scope verified.
- First consolidated full FAILED:1 failed,2977 passed,35 skipped,7 warnings in
  252.24s, exit1. Log:/tmp/dream035-final-adaptation-readiness.log. The sole failure
  is the obsolete PASS fixture response, independently replayed by the same judge.
- Managed focused correction attempt:exit124 after60s, log only F, no completed
  summary/traceback. Exact cause unknown. Earlier executor limitations do not prove
  this attempt's cause. Log:/tmp/dream035-background-verifier-correction-tests.log.
- SAME phase15 correction gate PASS:41 focused tests/1 explicit browser deselection
  in0.68s and2 independent old/corrected fixture replays in0.77s, both exit0.
  Report:/tmp/dream035-background-verifier-correction-gate.md. No product edits or
  weakened assertions. Prior marker-negative cases remain unchanged and pass.
- Tracking passed37 dated records/19 changed paths before the final consolidated
  retry. Baseline:/tmp/dream035-settings-attribution-baseline.json.
- Corrected consolidated full PASSED: **2,978 passed, 35 skipped, 7 warnings in
  262.48s**, exit 0. Log: /tmp/dream035-final-adaptation-corrected.log. All previously
  deselected browser cases were included. Skips: 34 explicit model-loading guards
  and one migration-state fixture. Warnings: two dependency deprecations, two
  synchronous-test asyncio marks, three SDK evaluator read-tool shadow warnings.
  No telemetry guard error or test failure was reported.
- Final source hash verification retained all eleven accepted phase16/17 and
  fixture-correction hashes plus the accepted phase15 verifier test hash.

## Unfinished work

None within this bounded source/review/integration checkpoint. DREAM-035's broader
goal remains active: live provider/model behavior, native Desktop, real wheel/clean
installation, actual sandbox/runtime readiness and owner acceptance are unqualified.
No perfection, world superiority or production acceptance is claimed. No owned
verification process remains; sessions70411,93463 and42755 have ended.

## Next steps

Continue DREAM-035 only from a reproduced defect or a declared qualification
contract. Use the existing Claude/Grok review packet for external feedback; no
external feedback was obtained. Keep live model loads, hardware/GPU probes,
benchmarks, builds/installs and owner-process changes deferred under the existing
scope. New Council backend controls can be exercised in the next normally started
Dream application; refresh alone cannot install new Python code.

This handoff is finalized after tracking. A later change requires a new dated
record and baseline; do not silently rewrite this history or blanket restore HEAD.
Failed evidence remains available. Source changes stay uncommitted and unpublished.

## Files changed

- `dream/core/engine.py`
- `dream/gui/static/controls.js`
- `dream/core/execution.py`
- `dream/core/script_execution.py`
- `dream/tools/studio.py`
- `dream/tools/native.py`
- `tests/test_execution_readiness.py`
- `tests/test_execution_readiness_ui.py`
- `docs/execution.md`
- `dream/core/backends/openai_compat.py`
- `tests/test_active_settings.py`
- `tests/test_background_filing.py`
- `docs/capability-contract.md`
- `docs/runtime-controls.md`
- `docs/harness-review-packet.md`
- `docs/project/CURRENT.md`
- `docs/project/MASTER_PLAN.md`
- `docs/superpowers/plans/2026-09-10-harness-adaptation-loop.md`
- `docs/project/updates/2026-09-10-harness-settings-attribution.md`

Phase17 finite acceptance and eight contributor paths declared before source edits
in the shared plan. It runs in separate /tmp/dream035-execution-readiness-workspace,
514 current-source files plus linked existing dependencies. Model settings and
readiness contributors own disjoint paths; each gets a new independent gate. Root
uses one handoff/common phase16 baseline for concurrent tracking, not separate
agent backlogs. No backend-adapter edits by readiness contributor. No new runtime
probe, tool filtering, diagnostics row or permission-policy change is authorized.

Phase16 author progress: both exact old display defects failed new assertions using
real backend MockTransport payload/admission evidence. First isolated green28 passed
with4 original browser cases explicitly deselected in0.34s. Only the declared
generation_settings_status function and Active settings tests changed. Source labels
identify limiting measured/reported/configured bounds including ties; accepted
current reasoning ladders permit bounded custom labels. Scope/final regressions
and new independent gate remain pending. No request arithmetic change allowed.

Phase16 broader CPU selection106 passed/4 browser deselections in0.60s. A lead
question about possible minimum-window clamping was resolved by source inspection:
RuntimeProfile.window validates positive integer measured context and takes its
minimum with the profile limit; it imposes no minimum clamp. That concern is not
a new defect. Author adds explicit1-token attribution cases before freeze without
altering request/admission logic. No gate or full-source pass claimed yet.

Phase16 candidate frozen, final109 tests passed/4 explicit browser deselections
in0.61s, no warnings. Author /tmp/dream035-settings-attribution-impl.md. Root
verified514 manifest entries, exactly2 allowed changes and main originals, wrote
-lead.diff/-lead-hashes.json, and dispatched NEW astra_settings_attribution_gate.
Judge also traces actual n_ctx origins so configured/environment fallback cannot
be mislabeled measured. A message to the completed author hit runtime thread limit;
no source or review scope changed, candidate remains frozen as author reported.

Phase17 red evidence:54 expected failures/12 unchanged controls passed in0.96s,
/tmp/dream035-execution-readiness-red.log. No setup/teardown error or forbidden
worker/probe attempt. New fake Python/DOM tests expose missing checked/snapshot
metadata, unavailable subtype/guidance and misleading UI wording. Author now edits
only six declared isolated production files. Main production remains unchanged.

Phase16 fresh gate FAIL, one concrete provenance finding. Real connect/_probe_n_ctx
with fake /props503, missing n_ctx or malformed n_ctx and DREAM_MACHX_CTX=6000
stores fallback6000 then labels it Measured backend context although reported
context is unknown. Judge109 tests passed/4 browser deselections plus321 checks
with3 provenance counterexamples and0 telemetry attempts. Main remains original.
Correction returns to SAME astra_settings_attribution_gate. Preserve the existing
display-only scope: do not certify measurement when n_ctx origin was not retained,
and never infer historical provenance from the current environment value. Opaque
backend bounds may remain explicitly unattributed; actual reported/profile sources
remain identified. No request, probe or admission change is authorized.

Phase17 candidate frozen:66 tests passed0.86s, no warnings/skips; exactly6 existing
production files and2 new tests changed. /tmp/dream035-execution-readiness-impl.md
and -diff/-hashes artifacts. New independent gate remains to dispatch. Author
corrected the red-log wrapper caveat: red54 failures/12 controls are observed, but
its wrapper ended with tail and did not retain pytest exit separately. No red pass
is claimed. Green command retains true exit0. No main readiness integration yet.

Lead checked phase17's514 baseline entries, exactly6 modified production files
and2 declared new tests, each main original unchanged. Exact -lead.diff and
-lead-hashes.json define the gate input. NEW astra_execution_readiness_gate now
independently tests finite scope/error/UI contracts. Phase16 author separately
corrects its first gate finding; no overlapping file ownership or source integration.

Phase16 correction frozen: backend bound now explicitly Unattributed backend
context; no historical source inference from current environment. Three real
fake-connect /props fallback regressions first failed the previous candidate, then
passed; each changes/removes the environment after connect and retains exact6000
status with only one fakeGET. Final112 passed/4 browser deselections in0.62s.
Author /tmp/dream035-settings-attribution-correction.md; previous failed artifacts
untouched. Root checked514 entries,2 changed paths/main originals and wrote new
-correction-lead.diff/-correction-lead-hashes.json. SAME judge now rechecks; report
pending /tmp/dream035-settings-attribution-correction-gate.md. No main integration.

SAME phase16 judge correction PASS:112 fresh tests/4 explicit browser deselections
and477 independent assertions,0 telemetry attempts. Report
/tmp/dream035-settings-attribution-correction-gate.md. False measurement claim is
resolved without request/admission/probe changes; exact scope/hashes verified.
Initial FAIL remains preserved. Main integration waits for phase17 final report
so both reviewed disjoint changes can enter consolidated verification together.

Phase17 fresh gate progress:66 tests passed0.82s, independent Python355 checks
including514-file integrity and45 prompt/status combinations, plus2188 Node
malformed/legacy/UI/countdown cases. All forbidden/telemetry attempts0. No finding
so far; final original-counterexample checks/report pending, not a final verdict.

Phase17 fresh gate PASS, zero blocking findings. Final report
/tmp/dream035-execution-readiness-gate.md:66 fresh tests0.82s,357 independent
Python assertions and2191 Node cases; all forbidden/telemetry attempts0. Exact
eight-file scope/hashes and outside-symbol bytes verified. Saved evidence, error
classification and legacy/red-team UI behavior meet the declared contract; this
does not qualify real containment/provider/native behavior.

Lead verified all ten original main/candidate hashes across disjoint phases16/17,
then integrated exactly those accepted files. New test paths were absent before
copy; no other source changed. Phase15 marker code remains in the accepted backend.
Consolidated full verification follows; no new full pass is claimed yet.

Consolidated full FAILED at 2026-09-10T23:50:40-05:00:1 failed,2977 passed,35 skipped,
7 warnings in252.24s, exit1. Log:/tmp/dream035-final-adaptation-readiness.log.
Owned session70411 ended. All previously deselected browser cases ran without
reported failures. The sole failure is the background-filing ordering fixture:
its fake verifier returns PASS fixture, contradicting phase15 exact-marker input,
then searches for a clean PASS event. Graph search returned Transport closed;
focused source read established this exact mismatch.

Corrective scope declared BEFORE edit: root changes only the single fake response
literal in tests/test_background_filing.py from PASS fixture to PASS. Preserve all
ordering, queue and pending assertions, every other test byte, and all product
hashes. This test measures required-verifier ordering, not marker classification;
phase15 already retains explicit contradictory-prefix negative cases. Verify the
focused background/verifier suites, then return to SAME phase15 Astra reviewer.
Original test bytes/hash saved in /tmp/dream035-background-verifier-precorrection.py
and -correction-hashes.json. No timeout increase, assertion removal, test skip or
production change. Current handoff and common baseline remain active.

Managed focused correction attempt ended exit124 after60s with only a failure
marker and no traceback/summary. Log:/tmp/dream035-background-verifier-correction-tests.log.
It is incomplete, not a pass; its exact failure cause was not captured. Earlier
managed asyncio executor limitations motivate using the approved execution path,
not a product-cause claim. SAME phase15 judge confirmed the one-literal AST/diff
and retained assertions, and now owns focused independent verification. No lead
duplicate is running. Tracking passed37 records/19 changed paths after the scope
addition. Operator docs also replace older generic measured-window descriptions
with stored/retained bounds, consistent with the corrected provenance contract.


SAME phase15 correction gate finalized PASS; root freshly read its report and
verified all eleven accepted hashes. The corrected full run started under approved
escalation as session42755. Original failed full and incomplete managed logs remain
unchanged. No new test exclusion or product change was introduced.


## Final integration evidence

Recorded at 2026-09-11T00:03:55-05:00. Corrected consolidated run ended exit 0 with
2,978 passed, 35 skipped and 7 warnings in 262.48s. No new exclusion or timeout
change was used. Exact command from the main checkout:

```bash
DREAM_ROOT="$(mktemp -d /tmp/dream035-final-adaptation-corrected-XXXXXX)" PYTHONPATH=/tmp/dream035-astra-preview-diagnostic PYTHONDONTWRITEBYTECODE=1 timeout 360s .venv/bin/python -m pytest -q -ra --tb=short -p no:cacheprovider -p dream035_cpu_preview_diag > /tmp/dream035-final-adaptation-corrected.log 2>&1
```

Automatic approval allowed the bounded run outside the managed executor. It used
the existing telemetry guards and Chromium --disable-gpu plugin; these are bounded
known-path protections, not universal GPU I/O isolation. Standard fixtures include
owned loopback/browser/subprocess work and temporary Git-repository operations.
No main-repository staging/commit, installation, live model/provider call, benchmark
or owner-process operation was performed. Independent gates and all failed/
incomplete attempts above remain distinct from this final result.

All eleven reviewed phase16/17 and fixture-correction hashes, plus the phase15
verifier test hash, were freshly checked after the run. The same phase15 judge
already verified the retained verifier method despite the separate phase16 edit.
Final tracking result is recorded after the final documentation update.

Final tracking first failed because completion crossed local midnight and the
lead changed Recorded to September11 while retaining the September10 filename.
The header is restored to its last validated September10 timestamp; the actual
September11 completion timestamp remains explicit in Final integration evidence.
This is a bookkeeping correction, not a source/test failure or changed completion
time. No previous finalized handoff was renamed or edited.

Final tracking PASSED: 37 dated records, all 19 changed source paths checked
against /tmp/dream035-settings-attribution-baseline.json. The header-date correction
resolved the prior tracking errors. This bounded checkpoint is finalized; the
broader DREAM-035 goal remains active and owner acceptance remains unrecorded.
