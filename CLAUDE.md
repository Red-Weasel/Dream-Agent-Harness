# Claude Code instructions — Dream

Follow [AGENTS.md](AGENTS.md). Read a file before editing it, preserve unrelated changes, and report the
verification you actually ran. Never commit runtime state (`data/`, `memory/`, `var/`, `.remember/`, logs, databases).
